import type { PackageItems } from '../types';

export const ASSET_ITEMS: PackageItems = {
  icon: {
    name: 'i-ic-baseline-landscape',
    color: 'text-purple-400',
  },
  canAdd: true,
  canDelete: true,
  canImport: true,
};
